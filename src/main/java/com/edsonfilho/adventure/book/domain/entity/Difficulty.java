package com.edsonfilho.adventure.book.domain.entity;

/**
 * Represents the difficulty level of an adventure book.
 * Maps to the "difficulty" field in the JSON files.
 */
public enum Difficulty {
    EASY,
    MEDIUM,
    HARD
}
